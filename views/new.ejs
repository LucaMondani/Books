<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
    </head>
    <body>
        <main>
            <% if (!locals.book) {%>
                <form action="/new" method="post"> 
                    <input name="isbn" type="text">
                    <textarea name="review" rows="5" cols="35"></textarea>
                    <input name="rating" min="1" max="10" type="number">
                    <button type="submit">Submit</button>
                </form>
                <% if(locals.error) { %>
                    <%= error %>
                <% } %>
            <% } else if (locals.book) {%>
                <form action="/edited" method="post">
                    <input type="hidden" name="book_id" value="<%= book.id %>"> 
                    <input name="isbn" type="text" value="<%= book.isbn %>" disabled>
                    <textarea name="review" rows="5" cols="35"><%= book.review %></textarea>
                    <input name="rating" min="1" max="10" type="number" value="<%= book.rating %>">
                    <button type="submit">Submit</button>
                </form>
                <% if(locals.error) { %>
                    <%= error %>
                <% } %>
            <% } %>
<%- include("partials/footer.ejs"); -%>     
    
